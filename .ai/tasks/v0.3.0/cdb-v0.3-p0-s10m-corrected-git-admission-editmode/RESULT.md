# Result: cdb-v0.3-p0-s10m-corrected-git-admission-editmode

## Outcome

- Status: `BLOCKED` during the sole bounded post-run Unity-log extraction.
- Date: `2026-09-08` (`Asia/Shanghai`).
- Actual model/profile: GPT-5 Codex session dispatched as `v0.3.coder.deep`, `GUARDED`, `REUSE_ONLY`; exact backend variant and reasoning-effort label are unavailable to this session.
- Human authorization: the current Planner dispatch records explicit authorization for this frozen one-shot corrected Unity/EditMode task.
- The corrected admission completed successfully. It selected one concrete scalar Git executable from two valid discovery records and preserved the exact NUL-delimited ten-path porcelain-v1 status contract.
- The one synchronous Unity process exited normally with code `0` after `53.0254733s` while the wrapper waited on the exact returned process object.
- The single XML parse passed every frozen result and coverage requirement: `69/69`, zero failed/skipped/inconclusive, exactly `39` represented methods, and no omitted or extra filter mapping.
- The single log read proved one exact repository-local Package record and zero compiler/fatal markers, but the frozen normal-completion pattern matched zero lines. That unmet log precondition triggered the first stop condition. No second pattern, investigation, retry, or remaining post-run check followed.

## Predecessor And Admission Evidence

- S10l helper failure disposition: historical `BLOCKED` before Unity launch because two Git executable paths were combined into one invalid `ProcessStartInfo.FileName`.
- S10k accepted decision confirmed the corrected lifecycle schema expectation and frozen lifecycle-test identity.
- The three predecessor records were each read once and matched their frozen lengths and SHA-256 identities.
- Admission command: exit `0`, wall time `0.9360652s`; output complete and untruncated (`64` tool-reported tokens).
- HEAD: `6408b0d540b32584147588efb67ecc5ba12b2fda`.
- Ten tracked input lengths and SHA-256 values: `10/10` matched the task card.
- Corrected Git selection: two valid application records were discovered; exactly one existing scalar executable path was selected locally. Its machine path was not printed or persisted.
- Scoped NUL-delimited porcelain-v1 status: exactly seven ` M` paths and three clean paths, matching the normalized unordered frozen set.
- Unity project: `UnityValidationProject/`, version `2022.3.47f1`, revision `88c277b85d21`.
- Unity editor registration: exactly one matching current-user Hub record; executable existed. Machine paths are omitted.
- Admission matching-Unity process count: `0`.
- Both fresh S10m artifact paths were absent before launch.
- Manifest/lock contract: dependency and lock version `file:../../com.rice.ai-codedb`; one `com.rice.ai-codedb` testable; lock `source: local`, `depth: 0`, zero dependencies; resolved source equaled the repository Package.
- Filter: `39` total and distinct methods; Lifecycle/Manager split `22/17`; Manager declaring-class split `10/3/4`.

## Single Unity Invocation

Sanitized command shape:

```powershell
$unityProcess = Start-Process -FilePath $unityEditor `
  -ArgumentList @(
    '-batchmode',
    '-projectPath', '<UnityValidationProject/>',
    '-runTests',
    '-testPlatform', 'EditMode',
    '-testFilter', '<exact frozen 39-name semicolon filter>',
    '-testResults', '<UnityValidationProject/TestResults-S10m-control-plane.xml>',
    '-logFile', '<UnityValidationProject/Logs/S10m-control-plane-corrected-editmode.log>') `
  -PassThru
$completed = $unityProcess.WaitForExit(300000)
```

- Invocation count: exactly `1`.
- Wait ownership: exact returned process object; no global polling while running.
- Wait result: completed before `300000ms`.
- Real Unity exit code: `0`.
- Exact-process wall time: `53.0254733s`.
- Wrapper evidence: `WAITED_EXACT_PROCESS=True`.
- Captured console output: one sanitized status line (`21` tool-reported tokens), complete and untruncated.
- No `-quit`, `-nographics`, hidden window, background job, detached helper, Unity Hub launch, Unity MCP, alternate endpoint, second invocation, termination, or signal was used.

## XML Evidence

The S10m XML was parsed structurally exactly once during the bounded post-run command. All of these assertions passed before the later log stop:

| Criterion | Result |
| --- | --- |
| Root result | `Passed` |
| Total / passed | `69 / 69` |
| Failed / skipped / inconclusive | `0 / 0 / 0` |
| Distinct declared method identities | `39` |
| Cases mapping to anything other than exactly one filter identity | `0` |
| Filter identities not represented | `0` |

- This is focused development EditMode evidence for the frozen `39 methods / 69 cases`; it is not manual UI, runtime, consumer, release, or publication acceptance.

## Targeted Log Evidence And Stop

The S10m log was read exactly once using the task's bounded extraction. The command reached this evidence before exiting `1` on the completion-marker requirement.

- Local `com.rice.ai-codedb` Package records: exactly `1`.
- Package source and location normalized to the repository Package: `True` for both fields.
- `error CS####` records: `0`.
- Immediate compiler-error summaries: `0`.
- Fatal/abort/crash markers: `0`.
- Lines matching the frozen normal focused-test completion pattern: `0`.
- Post-run command exit: `1`.
- Post-run command wall time: `0.7317825s`.
- Captured output: complete and untruncated (`78` tool-reported tokens).
- Stop reason: the log condition required at least one normal focused-test completion match. The XML and process exit were successful, but the task does not permit substituting those as the missing log-pattern evidence.
- No second log pattern, reread, broad search, or diagnostic inspection was performed.

## Not Completed / Deferred

- `NOT RUN`: post-run recomputation of the ten tracked input identities.
- `NOT RUN`: post-run NUL-delimited scoped-status comparison.
- `NOT RUN`: final point-in-time validation-project Unity process check.
- The exact launched Unity process itself returned exit `0` and its local process object was disposed, but no separate final system-process assertion is claimed.
- `NOT RUN`: retry, second Unity invocation, alternate filter, broader EditMode, compiler command, L0/L1 harness, source/test/config change, failure investigation, artifact deletion, or process termination.
- `NOT RUN` / `DEFERRED`: manual Unity UX, PlayMode, cold start, Domain Reload, real Manager/Supervisor/PowerShell/Node/Codex acceptance, consumer/third-party Package, full regression, release, publication, and deployment.
- No source, test, Package/configuration, validation setting, workflow, predecessor record, or tracked input was intentionally modified. Existing worktree state was preserved.
- No commit, push, stash, reset, clean, rebase, or amend occurred.

## Budget Ledger

- Admission: `1/1`, passed.
- Focused Unity L1 batch: `1/1`; Unity exit `0`.
- Post-run extraction: `1/1`, stopped at the normal-completion log marker.
- Retry: `0/0`.
- Unity wait: `53.0254733s / 300s`.
- Captured tool output: admission `64`, Unity `21`, post-run `78` reported tokens; no truncation and below all task limits.
- Allowed generated artifacts:
  - `UnityValidationProject/TestResults-S10m-control-plane.xml`;
  - `UnityValidationProject/Logs/S10m-control-plane-corrected-editmode.log`.
- This `RESULT.md` is the only durable task record written.

## Completion Routing

- Current task: `cdb-v0.3-p0-s10m-corrected-git-admission-editmode`.
- Current status: `BLOCKED` only because the sole bounded log extraction found zero lines matching its normal focused-test completion pattern.
- Return to: `UnityCodeDB v0.3 Planner`.
- Next role/action: Planner must review the passing exact-process, XML `69/69`, Package, compiler, and fatal evidence alongside the missing log-marker match, then decide whether to accept that residual evidence gap or authorize a separate read-only log-evidence task. Do not route to Verifier from this Coder task without Planner acceptance.
- Verifier contact: not performed.
- Commit/push: not performed.

当前 S10m 已取得 Unity exit `0`、精确 `39 methods / 69 cases` 全通过、repository Package 精确解析及零 compiler/fatal 证据，但因冻结日志 completion pattern 命中 `0` 而按 stop condition 标记 `BLOCKED`。下一步由 `UnityCodeDB v0.3 Planner` 复审并决定是否接受该证据缺口或授权独立只读日志证据任务。
